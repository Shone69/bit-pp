function threeDig(num) {
    if (num > 99 && num < 1000) {
        console.log(num + " is a three-digit number.");
    } else {
        console.log(num + " isn't a three-digit number.")
    }
}

threeDig(58);
threeDig(678);
threeDig(1058);